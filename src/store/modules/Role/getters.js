export const isRoleLoading = (state) => {
    return state.roleLoading;
};

export const roleError = (state) => {
    return state.role_error;
};  

export const getRoles = (state) => {
    return state.roles;
};
