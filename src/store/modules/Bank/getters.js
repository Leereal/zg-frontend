export const isBankLoading = (state) => {
    return state.bankLoading;
};

export const bankError = (state) => {
    return state.bank_error;
};  

export const getBanks = (state) => {
    return state.banks;
};
