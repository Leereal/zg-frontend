export const isGroupLoading = (state) => {
    return state.groupLoading;
};

export const groupError = (state) => {
    return state.group_error;
};  

export const getGroups = (state) => {
    return state.groups;
};
