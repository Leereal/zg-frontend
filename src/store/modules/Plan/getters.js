export const isPlanLoading = (state) => {
    return state.planLoading;
};

export const planError = (state) => {
    return state.plan_error;
};  

export const getPlans = (state) => {
    return state.plans;
};
